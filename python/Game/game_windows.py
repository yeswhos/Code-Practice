import pygame

pygame.init()
#set_mode(resolution = (0, 0), flags = 0, depth = 0)
#resolution屏幕宽和高
#flag是否全屏等等
#depth颜色的位数，自动匹配

screen = pygame.display.set_mode((480, 700))

while True:
    pass

pygame.quit()