def measure():
    temp = 39
    wetness = 50
    return(temp, wetness)

result = measure();
print(result)
# print(result[0])
# print(result[1])

gl_temp, gl_wetness = measure()
print(gl_temp, gl_wetness)
